name: ci

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Check out the reviewed Dovecote revision
        uses: actions/checkout@v6
        with:
          repository: plethu/dovecote
          ref: aec64deb018fd0820697457c3077ff0c76e2f0ef
          path: .ci/dovecote

      - name: Expose Dovecote at the local path used by Cargo
        run: ln -s "$GITHUB_WORKSPACE/.ci/dovecote" "$GITHUB_WORKSPACE/../carrier"

      - name: Install project tools
        uses: jdx/mise-action@v4

      - name: Cache cargo registry and build artifacts
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: gates-${{ hashFiles('.mise.toml', '.config/mise/conf.d/batten.toml', 'Cargo.lock') }}
          restore-keys: |
            gates-

      - name: Run project gates
        run: mise run check

  database-gates:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17.6-alpine
        env:
          POSTGRES_DB: keepsake
          POSTGRES_USER: keepsake
          POSTGRES_PASSWORD: keepsake
        ports:
          - 55432:5432
        options: >-
          --health-cmd "pg_isready -U keepsake -d keepsake"
          --health-interval 2s
          --health-timeout 2s
          --health-retries 20
      mysql:
        image: mysql:8.4
        env:
          MYSQL_DATABASE: keepsake
          MYSQL_USER: keepsake
          MYSQL_PASSWORD: keepsake
          MYSQL_ROOT_PASSWORD: keepsake-root
        ports:
          - 53306:3306
        options: >-
          --health-cmd "mysqladmin ping -h 127.0.0.1 -u keepsake -pkeepsake --silent"
          --health-interval 2s
          --health-timeout 2s
          --health-retries 30
    env:
      DATABASE_URL: postgres://keepsake:keepsake@localhost:55432/keepsake
      MYSQL_DATABASE_URL: mysql://keepsake:keepsake@localhost:53306/keepsake
      TEST_DB_UP: "0"
    steps:
      - uses: actions/checkout@v6

      - name: Check out the reviewed Dovecote revision
        uses: actions/checkout@v6
        with:
          repository: plethu/dovecote
          ref: aec64deb018fd0820697457c3077ff0c76e2f0ef
          path: .ci/dovecote

      - name: Expose Dovecote at the local path used by Cargo
        run: ln -s "$GITHUB_WORKSPACE/.ci/dovecote" "$GITHUB_WORKSPACE/../carrier"

      - name: Install project tools
        uses: jdx/mise-action@v4
        with:
          install_args: rust just

      - name: Cache cargo registry and build artifacts
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: database-gates-${{ hashFiles('.mise.toml', '.config/mise/conf.d/batten.toml', 'Cargo.lock') }}
          restore-keys: |
            database-gates-

      - name: Allow Dovecote trigger creation on the disposable MySQL service
        run: >-
          docker run --rm --network host mysql:8.4 mysql
          -h127.0.0.1 -P53306 -uroot -pkeepsake-root
          -e "SET GLOBAL log_bin_trust_function_creators = 1"

      - name: Run database gates
        run: mise run test-db
